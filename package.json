{

  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "gh-pages -d dist"
  },
  "devDependencies": {
    "@nuxt/content": "^2.4.2",
    "nuxt": "^3.1.1"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
